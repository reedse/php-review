<?php
/**
 * Exercise 3: FizzBuzz
 * 
 * Task:
 * Implement the classic FizzBuzz problem:
 * 
 * 1. Write a fizzBuzz() function that returns an array of values from $start to $end where:
 *    - For numbers divisible by 3, use "Fizz" instead of the number
 *    - For numbers divisible by 5, use "Buzz" instead of the number
 *    - For numbers divisible by both 3 and 5, use "FizzBuzz" instead of the number
 *    - For other numbers, use the number itself
 * 
 * 2. Write a printFizzBuzz() function that outputs the results directly to the console
 *    rather than returning an array
 */

function fizzBuzz($start = 1, $end = 100) {
    // TODO: Implement this function to return an array with FizzBuzz results
}

function printFizzBuzz($start = 1, $end = 100) {
    // TODO: Implement this function to print FizzBuzz results directly
}

// Example test code:
// $fizzBuzzResults = fizzBuzz(1, 20); // Get results as array
// print_r($fizzBuzzResults);
// 
// printFizzBuzz(1, 20); // Print results directly 